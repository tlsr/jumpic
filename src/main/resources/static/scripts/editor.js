/*
let but = document.querySelector("#addQuizButton");
/!*var data = {
    question: document.querySelector('form').seri
}*!/
console.log(but);
but.addEventListener("submit", (event) => {
    event.preventDefault();
    let data = document.querySelector("#questionField").value;
    console.log(data);
    let options = {
        method: 'POST',
        body: "question="+data
    }
    console.log(options.body);
    fetch('/editor', options);
    console.log("button pressed");
})
*/
